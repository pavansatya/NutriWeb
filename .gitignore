# Built catalog artifacts — rebuild with pipeline/, or pull from the Hub
data/*.duckdb
data/*.duckdb.wal
data/*.parquet

# Secrets — never commit. Set via HF Space "Repository secrets".
.streamlit/secrets.toml
.env

# Python
__pycache__/
*.py[cod]
.venv/
env/
.pytest_cache/
*.egg-info/

# macOS
.DS_Store
